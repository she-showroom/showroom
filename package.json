{
  "name": "she",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "parcel": "cp -r src/img dist && parcel src/index.html",
    "build": "rm -rf public && mkdir public && cp -r src/img public/img && parcel build --no-source-maps src/index.html -d public",
    "host": "firebase serve",
    "deploy": "firebase deploy",
    "init": "node tools/init.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/she-showroom/showroom.git"
  },
  "author": "",
  "license": "ISC",
  "homepage": "https://showroom.gallery",
  "dependencies": {
    "firebase": "^7.14.6",
    "firebase-tools": "^8.4.1"
  },
  "devDependencies": {
    "elm-hot": "^1.1.4",
    "node-elm-compiler": "^5.0.4",
    "parcel-bundler": "^1.12.4",
    "firebase-admin": "^8.12.1"
  }
}
